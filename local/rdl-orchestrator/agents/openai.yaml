interface:
  display_name: "RDL Orchestrator"
  short_description: "Drive evidence-backed research and build sessions."
  default_prompt: "Use $rdl-orchestrator to drive the active RDL session to a terminal result."

policy:
  allow_implicit_invocation: false
