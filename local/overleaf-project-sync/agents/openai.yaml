interface:
  display_name: "Overleaf Project Sync"
  short_description: "Compare or explicitly update a local Overleaf project."
  default_prompt: "Use $overleaf-project-sync to compare an Overleaf project and apply any explicitly authorized overwrite, reusing authorization already given."

policy:
  allow_implicit_invocation: false
